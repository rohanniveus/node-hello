on:
  workflow_dispatch:
    inputs:
      foo:
        description: 'Test input'
        required: true

  
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: |
          echo "FOO: ${INPUT_FOO}"
        shell: bash
        env:
          MY_VAR: ${{ inputs.foo }} 
      
    
