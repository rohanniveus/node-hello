on:
  workflow_dispatch:
    inputs:
      foo:
        description: 'Test input'
        required: true

  
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Run a one-line script
        run: |
          echo '::warning:: this is a warning'
          echo "FOO: ${INPUT_foo}"
        
      
    
